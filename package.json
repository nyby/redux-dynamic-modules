{
    "name": "@nyby/redux-dynamic-modules",
    "publishConfig": {
        "access": "public"
    },
    "scripts": {
        "bootstrap": "lerna bootstrap --no-ci",
        "publish": "lerna publish",
        "publish:force": "lerna publish --force-publish",
        "build:prod": "lerna run build:prod",
        "test": "npm run build:prod && lerna run test"
    },
    "devDependencies": {
        "lerna": "^5.1.8",
        "prettier": "^2.7.1"
    },
    "version": "5.3.12"
}
